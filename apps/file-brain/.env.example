# File Brain - Docker Environment Configuration

# Typesense Configuration
TYPESENSE_API_KEY=xyz-typesense-key
TYPESENSE_HOST=localhost
TYPESENSE_PORT=8108
TYPESENSE_PROTOCOL=http
TYPESENSE_COLLECTION_NAME=files

# Tika Server Configuration (Docker-based)
TIKA_HOST=localhost
TIKA_PORT=9998
TIKA_PROTOCOL=http
TIKA_ENABLED=true
TIKA_CLIENT_ONLY=true

# Application Configuration
DEBUG=false
WATCH_PATHS=/data/documents
MAX_FILE_SIZE_MB=100
BATCH_SIZE=10
WORKER_QUEUE_SIZE=1000

# Development Settings
# For development, you may want to set:
# DEBUG=true
# TIKA_HOST=localhost  # When running Tika container separately
# TYPESENSE_HOST=localhost  # When running Typesense container separately